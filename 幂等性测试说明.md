# MDP 框架幂等性测试说明

## 概述

本框架提供了完整的消息幂等性保证机制，支持两种实现方式：
1. **内存实现**（MemoryIdempotentService）- 适合单机/测试环境
2. **Redis实现**（RedisIdempotentService）- 适合分布式/生产环境

## 自动选择机制

框架会根据配置自动选择实现：
- ✅ 如果配置了 Redis → 自动使用 RedisIdempotentService
- ✅ 如果没有配置 Redis → 自动使用 MemoryIdempotentService

## 测试环境准备

### 方式1：使用内存实现（无需额外配置）

直接运行测试即可，框架会自动使用内存实现。

**优点：**
- 无需安装 Redis
- 配置简单

**限制：**
- 仅支持单实例
- 不适合测试分布式场景

### 方式2：使用 Redis 实现（推荐）

#### 1. 启动 Redis

**Docker 方式（推荐）：**
```bash
docker run -d --name redis-mdp -p 6379:6379 redis:6.2
```

**Windows 本地安装：**
- 下载 Redis for Windows
- 启动 redis-server.exe

**验证 Redis 是否启动：**
```bash
redis-cli ping
# 返回 PONG 表示成功
```

#### 2. 配置 Redis 连接

在 `src/test/resources/application.yml` 中添加或修改：

```yaml
spring:
  redis:
    host: localhost
    port: 6379
    # password: your_password  # 如果有密码
    database: 0
```

或者使用独立的配置文件 `application-redis.yml`，然后激活 profile：
```yaml
spring:
  profiles:
    active: redis
```

## 测试场景

### 场景1：单实例幂等性测试

**测试方法：** `testIdempotentMessageConsume()`

**步骤：**
1. 发送3条相同的订单消息（orderId 相同）
2. 观察日志，验证只有1次处理成功

**预期结果：**
```
[第1次] 发送订单: ORDER_IDEMPOTENT_001
收到同步订单消息: OrderInfo{orderId='ORDER_IDEMPOTENT_001'...}
开始处理订单 [同步消息]: orderId=ORDER_IDEMPOTENT_001...
订单处理成功 [同步消息]: orderId=ORDER_IDEMPOTENT_001

[第2次] 发送相同订单（应该被去重）: ORDER_IDEMPOTENT_001
检测到重复消息 - businessKey: ORDER_IDEMPOTENT_001...
跳过重复消息 - MsgId: xxx, Method: sendOrder

[第3次] 发送相同订单（应该被去重）: ORDER_IDEMPOTENT_001
检测到重复消息 - businessKey: ORDER_IDEMPOTENT_001...
跳过重复消息 - MsgId: xxx, Method: sendOrder
```

### 场景2：并发幂等性测试

**测试方法：** `testConcurrentIdempotentConsume()`

**步骤：**
1. 快速连续发送10条相同订单消息
2. RocketMQ 会并发投递给多个消费线程
3. 验证分布式锁保证只有1个线程处理成功

**预期结果：**
- 只有1次 "订单处理成功"
- 其他9次被去重跳过

### 场景3：不同订单正常消费

**测试方法：** `testDifferentOrdersWithIdempotent()`

**步骤：**
1. 发送10条不同订单ID的消息
2. 验证幂等性不影响正常消息

**预期结果：**
- 10条消息都被处理
- 10次 "订单处理成功"

## Redis 实现验证

### 查看 Redis 中的去重记录

```bash
# 连接 Redis
redis-cli

# 查看所有幂等性记录
keys mdp:idempotent:*

# 查看具体记录
get mdp:idempotent:ORDER_IDEMPOTENT_001

# 查看过期时间（秒）
ttl mdp:idempotent:ORDER_IDEMPOTENT_001
```

### 清理测试数据

```bash
# 删除所有幂等性记录
redis-cli --scan --pattern "mdp:idempotent:*" | xargs redis-cli del
redis-cli --scan --pattern "mdp:lock:*" | xargs redis-cli del
```

## 分布式测试（多实例）

### 启动多个实例

**方式1：IDEA 多实例启动**
1. 复制 Run Configuration
2. 修改端口：`-Dserver.port=8081`
3. 同时启动多个实例

**方式2：命令行启动**
```bash
# 实例1
java -jar target/gaoji-common-mdp-enhanced-1.0.0.jar --server.port=8080

# 实例2
java -jar target/gaoji-common-mdp-enhanced-1.0.0.jar --server.port=8081
```

### 验证分布式幂等性

1. 启动2个实例（都连接同一个 Redis）
2. 在实例1发送消息
3. 观察两个实例的日志
4. 验证只有一个实例处理消息

## 关键日志

### 启动日志

**Redis 实现：**
```
初始化幂等性服务 - 使用 Redis 实现（RedisIdempotentService）
Redis 实现支持分布式部署，适合生产环境
```

**内存实现：**
```
初始化幂等性服务 - 使用内存实现（MemoryIdempotentService）
⚠️  内存实现不支持分布式部署，仅适合单机/测试环境
⚠️  生产环境建议配置 Redis 以使用 RedisIdempotentService
```

### 消息处理日志

**首次处理：**
```
开始处理消息 - businessKey: ORDER_IDEMPOTENT_001, messageId: xxx, timeout: 86400s
订单处理成功 [同步消息]: orderId=ORDER_IDEMPOTENT_001
标记消息处理成功 - businessKey: ORDER_IDEMPOTENT_001, messageId: xxx
```

**重复消息：**
```
检测到重复消息 - businessKey: ORDER_IDEMPOTENT_001, 原始messageId: xxx, 当前messageId: yyy
跳过重复消息 - MsgId: yyy, Method: sendOrder
```

**并发冲突：**
```
获取分布式锁失败，可能存在并发消费 - businessKey: ORDER_IDEMPOTENT_001, messageId: xxx
```

## 常见问题

### Q1: 如何确认使用了哪种实现？
**A:** 查看启动日志中的 "初始化幂等性服务" 信息。

### Q2: Redis 连接失败怎么办？
**A:** 
1. 确认 Redis 已启动：`redis-cli ping`
2. 检查配置：host、port、password
3. 如果无法连接 Redis，框架会降级使用内存实现

### Q3: 内存实现能用于生产环境吗？
**A:** 不建议。内存实现的问题：
- 不支持分布式（多实例无法共享状态）
- 重启丢失数据
- 无法跨实例去重

### Q4: 幂等性去重记录什么时候过期？
**A:** 
- 由 `@Idempotent` 注解的 `timeout` 参数控制
- 默认 86400 秒（24小时）
- Redis 使用 TTL 自动过期
- 内存实现每小时清理一次

### Q5: 如何自定义幂等性实现？
**A:** 实现 `IdempotentService` 接口，并注册为 Spring Bean：
```java
@Bean
public IdempotentService myIdempotentService() {
    return new MyCustomIdempotentService();
}
```

## 性能监控

### Redis 实现监控

```bash
# 查看去重记录数量
redis-cli dbsize

# 查看内存使用
redis-cli info memory

# 查看慢查询
redis-cli slowlog get 10
```

### 建议的监控指标

1. **去重记录数量** - 反映消息处理量
2. **Redis 内存使用** - 防止内存溢出
3. **锁冲突次数** - 反映并发冲突情况
4. **重复消息次数** - 反映消息重复投递情况

## 生产环境建议

1. ✅ 使用 Redis 实现
2. ✅ 配置 Redis 集群/哨兵（高可用）
3. ✅ 设置合理的 timeout（根据业务场景）
4. ✅ 监控 Redis 性能和内存
5. ✅ 定期检查重复消息日志
6. ✅ 压测验证幂等性性能

## 测试清单

- [ ] 单实例 - 重复消息去重
- [ ] 单实例 - 并发消息去重
- [ ] 单实例 - 不同订单正常消费
- [ ] 多实例 - 分布式去重（需要 Redis）
- [ ] 消费失败重试 - 去重记录被清除
- [ ] 过期时间验证 - 24小时后可重复处理
- [ ] Redis 重启 - 去重记录持久化
- [ ] 应用重启 - Redis 记录依然有效

祝测试顺利！🎉
